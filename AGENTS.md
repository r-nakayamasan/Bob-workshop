# Agents.md

## 目的

このリポジトリでは**仕様駆動開発**を行います。
IBM Bob はすぐに実装へ入らず、「何を作るか」を明確にしてから、小さく実装・検証します。

---

## 基本方針

* 仕様と設計を整理してからコードを書く
* 変更は必要最小限にする
* 書く前に既存コードを読む
* 実装後は必ず確認する
* 不明点や仮定は明示する
* 要件にないことを勝手に足さない

---

## 作業の流れ

**最初は必ず Plan モードで始める。**

1. **現状理解** — 依頼を整理し、関係ファイルを読む
2. **仕様書作成** — `specs/YYYYMMDD-<slug>-spec.md` に仕様を書く
3. **設計書作成** — `specs/YYYYMMDD-<slug>-plan.md` に設計を書く
4. **実装** — 設計書に従い、既存の設計に合わせて変更する
5. **検証** — lint / test / build / 手動確認（未確認は明示する）
6. **結果共有** — 変更内容・確認内容・残課題を簡潔にまとめる

> **注意:** ファイル名の `YYYYMMDD` 部分は必ず `date +%Y%m%d` コマンドを実行して正確な日付を取得すること。記憶や推測で日付を入力しない。

---

## 仕様書のルール

* 保存先: `specs/` フォルダ（リポジトリ直下）
* ファイル名: `YYYYMMDD-<slug>-spec.md`（例: `20250101-add-login-spec.md`）
* テンプレート:

```md
# 仕様書: <タイトル>

## 基本情報
- 種別: 新規作成 / 改修
- 作成日: YYYY-MM-DD
- 作成者:
- ステータス: Draft / Review / Approved

## 背景・目的
<!-- なぜこの変更が必要か。ビジネス上の文脈を含めて記述する -->

## 現在の挙動
<!-- 現状の動作を具体的に記述する。改修の場合のみ記載 -->

## 期待する挙動
<!-- 変更後にどう動くべきかを、確認可能な形で記述する -->

## 対象範囲
<!-- 変更するコンポーネント・モジュール・APIなどを列挙する -->

## 対象外
<!-- 今回の変更に含まれないもの・将来の課題を明示する -->

## 利用者・影響システム
<!-- エンドユーザー、連携システム、呼び出し元など -->

## 制約・非機能要件
<!-- パフォーマンス、セキュリティ、互換性、監査要件など -->

## 完了条件
<!-- 受け入れテストの観点で、「〜であればOK」の形で列挙する -->

## 確認方法
<!-- テスト種別・シナリオ・確認手順を記述する -->

## リスク・懸念点
<!-- 不明点・仮定・既知リスクを記述する -->
```

---

## 設計書のルール

* 保存先: `specs/` フォルダ（リポジトリ直下）
* ファイル名: `YYYYMMDD-<slug>-plan.md`（例: `20250101-add-login-plan.md`）
* テンプレート:

```md
# 設計書: <タイトル>

## 基本情報
- 仕様書: YYYYMMDD-<slug>-spec.md
- 作成日: YYYY-MM-DD
- 作成者:
- ステータス: Draft / Review / Approved

## アーキテクチャ概要
<!-- 変更対象のコンポーネント構成・依存関係を記述する -->

## 変更ファイル一覧
| ファイル | 変更種別 | 変更概要 |
|---|---|---|
| path/to/file.ts | 新規 / 修正 / 削除 | 変更の要約 |

## インターフェース設計
<!-- 変更するAPI・関数・型・スキーマを記述する -->
<!-- 追加/変更/削除のいずれかを明示する -->

## データ設計
<!-- DB変更・マイグレーション・データフロー（該当する場合） -->

## 処理フロー
<!-- 主要な処理の流れをステップ形式または擬似コードで記述する -->

## エラー処理方針
<!-- エラーケース・ログ・ユーザーへの通知方針 -->

## 実装手順
<!-- 小さく分けた実施ステップ。依存関係を考慮した順序で記述する -->
1.
2.
3.

## 互換性・移行計画
<!-- 既存機能・データへの影響、後方互換性の方針 -->

## 検証計画
<!-- テスト種別・シナリオ・担当・実施タイミング -->

## 未解決事項
<!-- 設計時に判断できなかった事項と、判断期日 -->
```

---

## 新規作成 vs 改修

| | 新規作成 | 改修 |
|---|---|---|
| 仕様書で重視すること | 入出力・APIの契約、セキュリティ・権限要件 | 現在の挙動の記録、呼び出し元・互換性への影響 |
| 設計書で重視すること | インターフェース設計、データ設計 | 変更差分、移行・ロールバック計画 |

どちらも: 影響範囲を確認 → 最小の変更で実施 → テストを追加・更新する

---

## 完了条件

* 実装が仕様書の完了条件に合っている
* 仕様書 (`specs/*-spec.md`) が存在し、Approved である
* 設計書 (`specs/*-plan.md`) が存在し、Approved である
* 変更範囲が設計書の「変更ファイル一覧」の範囲に収まっている
* 必要な確認が終わっている（未確認は明示）

---

## やってはいけないこと

* Plan モードを飛ばして実装を始める
* 仕様書・設計書を作らずに実装する
* 設計書の「変更ファイル一覧」にないファイルを無断で触る
* 関係ない変更を混ぜる
* 確認せずに完了扱いにする
* 不明点を隠したまま進める
* **作業の流れのステップを飛ばす** — 以下はすべて禁止:
  * 現状理解を省いて仕様書・設計書を書く
  * 仕様書・設計書のレビュー・承認を待たずに実装へ進む
  * 実装後に lint / test / build を実行しないまま完了と報告する（テストをスキップしない）
  * 結果共有を省いて作業を終える
* ファイル名の日付を記憶や推測で入力する — 必ず `date +%Y%m%d` で確認すること

---

## 最終報告の型

```md
## 結果
- 変更内容（設計書との差分があれば明記）

## 確認
- 実施した確認

## ドキュメント
- 仕様書: specs/YYYYMMDD-<slug>-spec.md
- 設計書: specs/YYYYMMDD-<slug>-plan.md

## 補足
- 注意点・未対応事項
```
