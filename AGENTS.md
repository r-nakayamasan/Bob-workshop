# Agents.md

## 目的

このリポジトリでは**仕様駆動開発**を行います。
IBM Bob はすぐに実装へ入らず、「何を作るか」を明確にしてから、小さく実装・検証します。

---

## 基本方針

* 仕様と実装計画を整理してからコードを書く
* 変更は必要最小限にする
* 書く前に既存コードを読む
* 実装後は必ず確認する
* 不明点や仮定は明示する
* 要件にないことを勝手に足さない

---

## 作業の流れ

**最初は必ず Plan モードで始める。**

1. **現状理解** — 依頼を整理し、関係ファイルを読む
2. **仕様書作成** — `specs/YYYYMMDD-<slug>-spec.md` に仕様を書く
3. **実装計画書作成** — `specs/YYYYMMDD-<slug>-plan.md` に実装計画を書く
4. **実装** — 実装計画書に従い、既存のコードに合わせて変更する
5. **検証** — lint / test / build / 手動確認（未確認は明示する）
6. **結果共有** — 変更内容・確認内容・残課題を簡潔にまとめる

---

## 仕様書のルール

* 保存先: `specs/` フォルダ（リポジトリ直下）
* ファイル名: `YYYYMMDD-<slug>-spec.md`（例: `20250101-add-login-spec.md`）
* テンプレート:

```md
# 仕様書: <タイトル>

## 基本情報
- 種別: 新規作成 / 改修
- 作成日: YYYY-MM-DD
- 作成者:
- ステータス: Draft / Review / Approved

## 背景・目的
<!-- なぜこの変更が必要か。ビジネス上の文脈を含めて記述する -->

## 現在の挙動
<!-- 現状の動作を具体的に記述する。改修の場合のみ記載 -->

## 期待する挙動
<!-- 変更後にどう動くべきかを、確認可能な形で記述する -->

## 対象範囲
<!-- 変更するコンポーネント・モジュール・APIなどを列挙する -->

## 対象外
<!-- 今回の変更に含まれないもの・将来の課題を明示する -->

## 利用者・影響システム
<!-- エンドユーザー、連携システム、呼び出し元など -->

## 制約・非機能要件
<!-- パフォーマンス、セキュリティ、互換性、監査要件など -->

## 完了条件
<!-- 受け入れテストの観点で、「〜であればOK」の形で列挙する -->

## 確認方法
<!-- テスト種別・シナリオ・確認手順を記述する -->

## リスク・懸念点
<!-- 不明点・仮定・既知リスクを記述する -->
```

---

## 実装計画書のルール

* 保存先: `specs/` フォルダ（リポジトリ直下）
* ファイル名: `YYYYMMDD-<slug>-plan.md`（例: `20250101-add-login-plan.md`）
* テンプレート:

```md
# 実装計画書: <タイトル>

## 基本情報
- 仕様書: YYYYMMDD-<slug>-spec.md
- 作成日: YYYY-MM-DD
- 作成者:
- ステータス: Draft / Review / Approved

## 変更ファイル一覧
| ファイル | 変更種別 | 変更概要 |
|---|---|---|
| path/to/file.ts | 新規 / 修正 / 削除 | 変更の要約 |

## 実装手順
<!-- 小さく分けた実施ステップ。依存関係を考慮した順序で記述する -->
1.
2.
3.

## 検証計画
<!-- テスト種別・シナリオ・確認手順 -->

## 未解決事項
<!-- 実装着手前に判断が必要な事項と、判断期日 -->
```

---

## 新規作成 vs 改修

| | 新規作成 | 改修 |
|---|---|---|
| 仕様書で重視すること | 入出力・APIの契約、セキュリティ・権限要件 | 現在の挙動の記録、呼び出し元・互換性への影響 |
| 実装計画書で重視すること | 実装手順の粒度、依存関係の順序 | 変更差分の最小化、移行・ロールバック手順 |

どちらも: 影響範囲を確認 → 最小の変更で実施 → テストを追加・更新する

---

## 完了条件

* 実装が仕様書の完了条件に合っている
* 仕様書 (`specs/*-spec.md`) が存在し、Approved である
* 実装計画書 (`specs/*-plan.md`) が存在し、Approved である
* 変更範囲が実装計画書の「変更ファイル一覧」の範囲に収まっている
* 必要な確認が終わっている（未確認は明示）

---

## やってはいけないこと

* Plan モードを飛ばして実装を始める
* 仕様書・実装計画書を作らずに実装する
* 実装計画書の「変更ファイル一覧」にないファイルを無断で触る
* 関係ない変更を混ぜる
* 確認せずに完了扱いにする
* 不明点を隠したまま進める
* `create_html_artifact` による調査レポートを作成する（作成ファイルと混乱を招くため）

---

## 最終報告の型

```md
## 結果
- 変更内容（設計書との差分があれば明記）

## 確認
- 実施した確認

## ドキュメント
- 仕様書: specs/YYYYMMDD-<slug>-spec.md
- 実装計画書: specs/YYYYMMDD-<slug>-plan.md

## 補足
- 注意点・未対応事項
```
