# AGENTS.md

## 目的

このリポジトリでは、IBM Bobを使用した**仕様駆動開発**を行います。

新規実装、追加改修、不具合修正のいずれも、要件をユーザーと確認し、仕様書と実装計画書の承認を得てから実装します。

---

## 基本方針

* 既存コードと関連ドキュメントを確認してから変更する
* 読み取れた内容も、ユーザーへの確認なしに確定しない
* ユーザーへの確認にはフォローアップクエスチョンツールを使用する
* 確認は原則として一問ずつ行う
* 各ステップの結果を提示し、承認後に次へ進む
* 変更は必要最小限とし、要件にない変更を加えない
* 実装後は必要な検証を行い、未確認事項やリスクを明示する

---

## 作業の流れ

最初は**Agentモード**で開始します。

### 1. 現状理解

最初に以下を確認します。

* `AGENTS.md`
* `README.md`
* 関連するコード、設定、テスト、ドキュメント
* `specs/spec-template.md`
* `specs/plan-template.md`

確認した内容をもとに、以下についてユーザーと認識を合わせます。

1. **目的と期待結果**
2. **現状と変更範囲**
3. **仕様と制約**
4. **完了条件と確認方法**

依頼内容や既存コードから読み取れた理解を提示し、正しいか確認します。

確認にはフォローアップクエスチョンツールを使用し、原則として一度に一つの質問だけを提示します。回答を受けて認識を更新してから、次の確認へ進みます。

内容が明確に見える場合も確認を省略しません。ただし、強く関連する事項は一つの質問にまとめることができます。

フォローアップクエスチョンツールが利用できない場合は、通常のチャットで一問ずつ確認します。

#### GUI対象の判定

現状理解の早い段階で、今回の変更がGUIを伴うかを必ず確認します。

以下のいずれかに該当する場合は、GUIを伴う可能性があるものとして扱います。

* 依頼に画面、ページ、フォーム、一覧、ダイアログ、ボタン、メニューなどが含まれる
* フロントエンドのコンポーネントやスタイルを変更する
* ユーザー操作や画面遷移が変わる
* 表示内容、配置、デザイン、レスポンシブ対応が変わる
* リポジトリにフロントエンドがあり、変更の影響が画面に及ぶ可能性がある

GUIを伴う可能性がある場合は、最初に次のように確認します。

> 今回の変更には、画面の新規作成または変更も含まれるという理解で正しいですか？

GUIを伴う場合は、少なくとも以下を確認します。

* 対象画面と表示内容
* 画面構成と要素の配置
* ユーザー操作、画面遷移、状態変化
* 入力条件とバリデーション
* 読み込み中、空状態、エラーなどの表示
* 既存デザインやデザインシステムへの準拠
* 対応端末と画面幅

GUIを伴わないとユーザーが明示した場合のみ、画面仕様の確認を省略できます。

文章だけで認識を合わせにくい場合は、ワイヤーフレーム、モック、画面構成図などを作成し、実装前に承認を得ます。

#### 現状理解の提示

確認後、以下の形式で現状理解を提示します。

```md
## 現状理解

### 目的と期待結果
- 目的:
- 作業種別:
- 期待する結果:

### 現状と変更範囲
- 現在の挙動:
- 対象範囲:
- 対象外:
- 利用者・影響システム:

### 仕様と制約
- 期待する挙動:
- 主な仕様:
- 制約・非機能要件:
- GUI: 対象 / 対象外

### 完了条件と確認方法
- 完了条件:
- 確認方法:

### 仮定・懸念点
-
```

GUIを伴う場合は、以下も追加します。

```md
## 画面に関する理解
- 対象画面:
- 画面構成・表示項目:
- 操作・画面遷移:
- 表示状態:
- 入力・バリデーション:
- デザイン要件:
- 対応端末:
- 参考資料:
```

個別の質問への回答は、現状理解全体への承認とは扱いません。

現状理解全体を提示し、承認を得てから仕様書作成へ進みます。

---

### 2. 仕様書作成

承認された現状理解をもとに、仕様書を作成または更新します。

```text
specs/YYYYMMDD-<slug>-spec.md
```

仕様書は、以下のテンプレートを使用します。

```text
specs/spec-template.md
```

追加改修や不具合修正の場合も、実装前に仕様書を作成または更新します。

* 既存仕様の範囲内の変更: 既存仕様書を更新する
* 新しい目的や完了条件を含む変更: 新しい仕様書を作成する
* 判断できない場合: ユーザーに確認する

GUIを伴う場合は、承認された画面仕様を仕様書に記載します。

仕様書を提示し、ユーザーの承認を得ます。承認前は`Draft`または`Review`、承認後は`Approved`とします。

---

### 3. 実装計画書作成

承認された仕様書をもとに、実装計画書を作成します。

```text
specs/YYYYMMDD-<slug>-plan.md
```

実装計画書は、以下のテンプレートを使用します。

```text
specs/plan-template.md
```

実装計画書には、変更対象ファイル、変更内容、実装手順、テスト、検証方法を記載します。

GUIを伴う場合は、承認済みの画面仕様を、どのコンポーネントやファイルで実装するかを記載します。

実装計画書を提示し、ユーザーの承認を得ます。承認前は`Draft`または`Review`、承認後は`Approved`とします。

---

### 4. 実装

承認された仕様書と実装計画書に従って実装します。

* 既存の設計、構成、命名、規約に合わせる
* 実装計画書に記載された範囲だけを変更する
* 関係のない変更やリファクタリングを混ぜない
* 必要なテストを追加または更新する
* GUIは承認済みの画面仕様に従う

実装中に以下が発生した場合は、実装を止めます。

* 新しい要件や不明点が判明した
* 仕様の変更が必要になった
* 変更対象ファイルの追加が必要になった
* 実装計画と異なる対応が必要になった
* 承認済みの画面仕様を変更する必要が生じた

必要な事項をユーザーに確認し、仕様書と実装計画書を更新します。更新内容の再承認を得てから実装を再開します。

---

### 5. 検証

実装後、利用可能な確認を行います。

* lint
* unit test / integration test
* type check
* build
* 手動確認
* 既存機能への影響確認
* 仕様書の完了条件との照合

GUIを伴う場合は、以下も確認します。

* 画面構成と表示内容
* 操作と画面遷移
* 読み込み中、空状態、エラーなどの表示
* 入力とバリデーション
* 対象端末と画面幅
* 既存デザインとの整合性

実施できなかった確認がある場合は、理由、残るリスク、必要な確認方法を明示します。

検証結果を提示し、ユーザーの確認を得てから完了とします。

---

### 6. 結果共有

変更内容、検証結果、作成したドキュメント、残課題を共有します。

---

## タイムスタンプ

仕様書と実装計画書を作成または更新する際は、`execute_command`で現在日時を取得して記録します。

* タイムゾーンを含むISO 8601形式で記録する
* ファイル名には取得日時の日付部分を使用する
* 新規作成時は、作成日時と更新日時に同じ値を記録する
* 更新時は、作成日時を維持し、更新日時を変更する
* ステータス変更時も更新日時を変更する
* 日時を手入力または推測しない

---

## 承認の扱い

質問への回答や追加情報の提供だけでは、ステップ全体の承認とは扱いません。

回答を反映した結果を改めて提示し、明確な同意を得た場合に承認済みとします。

承認確認にもフォローアップクエスチョンツールを使用できます。

`update_todo_list`でステップを完了とするのは、ユーザーの承認後に限ります。

---

## 完了条件

以下を満たした場合にタスクを完了とします。

* 現状理解と必要な確認が完了し、全体の承認を得ている
* 仕様書と実装計画書が`Approved`である
* 作成日時と最新の更新日時が記録されている
* 実装が仕様書の完了条件を満たしている
* 変更範囲が実装計画書と一致している
* 必要な検証が完了している
* 未確認事項、残課題、既知のリスクが共有されている

---

## 禁止事項

* GUI対象かどうかを確認せずに現状理解を確定する
* 現状理解やユーザーへの確認を省略する
* 複数の確認事項を必要以上にまとめて質問する
* ユーザーの承認前に次のステップへ進む
* 仕様書・実装計画書の承認前に実装する
* 追加改修や不具合修正を理由に文書化を省略する
* GUI仕様を定義せずに画面を実装する
* 不明点を独自の仮定で確定する
* 実装計画書にない範囲を無断で変更する
* 要件にない機能やリファクタリングを追加する
* 未検証の内容を確認済みとして報告する
* タイムスタンプを省略、手入力、推測する
* `create_html_artifact`で調査レポートを作成する

---

## 最終報告

```md
## 結果
- 変更内容:
- 仕様書・実装計画書との差分:

## 確認
- 実施した確認:
- 確認結果:
- 未実施の確認:

## ドキュメント
- 仕様書:
- 実装計画書:

## 補足
- 残課題:
- 注意点・既知のリスク:
```
