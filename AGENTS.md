# Agents.md

## 目的

このリポジトリでは**仕様駆動開発**を行います。
IBM Bob はすぐに実装へ入らず、まず「何を作るか」「何を変えるか」を明確にしてから、小さく実装・検証します。

---

## 基本方針

* 先に仕様を整理してからコードを書く
* 変更は必要最小限にする
* 書く前に既存コードを読む
* 実装後は必ず確認する
* 不明点や仮定は明示する
* 要件にないことを勝手に足さない

---

## 進め方

作業は原則として次の順で進めること。

1. 現状理解
2. 仕様整理
3. 実装計画
4. 実装
5. 検証
6. 結果共有

---

## 1. 現状理解

まず依頼内容を整理する。

確認すること:

* 新規作成か、既存改修か
* 何を実現したいか
* 明示された要件は何か
* 制約はあるか
* 不明点はどこか

コード変更前に、関係するファイルを読む。

対象例:

* 関連モジュール
* 入口となる処理
* テスト
* 設定ファイル
* インターフェース
* 既存ドキュメント

---

## 2. 仕様整理

実装前に、短くてもよいので仕様を書く。

最低限含めること:

* 目的
* 現在の挙動
* 望ましい挙動
* 対象範囲
* 対象外
* 完了条件
* 確認方法

曖昧な表現ではなく、確認可能な形で書くこと。

---

## 3. 実装計画

実装前に、変更の進め方を小さく分ける。

含めること:

* どのファイルを触るか
* どの順序で進めるか
* 何で確認するか
* リスクがどこにあるか

大きい変更は一気にやらず、段階的に進める。

---

## 4. 実装

実装時のルール:

* 既存の書き方や設計に合わせる
* 変更範囲を広げすぎない
* 無関係なリファクタは混ぜない
* 互換性を壊す場合は明示する
* 必要ならコメントを入れるが、冗長にはしない

---

## 5. 検証

変更内容に応じて確認する。

確認方法の例:

* 単体テスト
* 結合テスト
* E2E テスト
* lint
* type check
* build
* 手動確認

確認では以下を意識する:

* 正常系
* 異常系
* 境界値
* 既存機能への影響

確認できていないものがある場合は、未確認と明示する。

---

## 6. 結果共有

作業完了時は次を簡潔にまとめる。

* 何を変更したか
* なぜ変更したか
* 何を確認したか
* 残課題や注意点

---

## 新規作成時の手順

新機能や新モジュールを作る場合は次で進める。

1. 解決したい課題を整理する
2. 入出力や画面/APIの契約を先に決める
3. 受け入れ条件を決める
4. 最小の実装単位に分ける
5. コア処理から実装する
6. エラー処理を追加する
7. テストを書く
8. 必要ならドキュメントを更新する

見るべき観点:

* セキュリティ
* 性能
* 権限
* 監視やログ
* 使いやすさ
* 既存機能との整合

---

## 改修時の手順

既存機能の変更やバグ修正では次を重視する。

1. 現在の挙動を再現・確認する
2. どこが問題かを特定する
3. 現状と理想の差分を明文化する
4. 影響範囲を確認する
5. 最小の修正で直す
6. 再発防止のためのテストを追加する
7. 周辺機能が壊れていないか確認する

特に確認すること:

* 呼び出し元への影響
* API互換性
* データ互換性
* UIやメッセージの変化
* 既存テストへの影響

---

## 仕様テンプレート

```md
## 仕様
- 種別: 新規作成 / 改修
- 目的:
- 現在の挙動:
- 期待する挙動:
- 対象範囲:
- 対象外:
- 完了条件:
- 確認方法:
```

---

## 計画テンプレート

```md
## 計画
1. 関連ファイルを確認する
2. 仕様を整理する
3. 最小変更で実装する
4. テストを追加・更新する
5. 確認を実施する
6. 結果と懸念点を共有する
```

---

## 完了条件

以下を満たしたら完了とする。

* 実装が仕様に合っている
* 変更範囲が適切である
* 必要な確認が終わっている
* 未確認事項が明示されている
* 必要なら関連ドキュメントが更新されている

---

## やってはいけないこと

* 仕様整理なしで実装を始める
* 関係ない変更を混ぜる
* 確認せずに完了扱いにする
* 不明点を隠したまま進める
* 必要以上に大きく作る

---

## 最終報告の型

```md
## 結果
- 変更内容

## 確認
- 実施した確認

## 補足
- 注意点
- 未対応事項
```
