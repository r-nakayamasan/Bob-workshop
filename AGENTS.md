# Agents.md

## 目的

このリポジトリでは**仕様駆動開発**を行います。
IBM Bob はすぐに実装へ入らず、「何を作るか」を明確にしてから、小さく実装・検証します。

---

## 基本方針

* 仕様を整理してからコードを書く
* 変更は必要最小限にする
* 書く前に既存コードを読む
* 実装後は必ず確認する
* 不明点や仮定は明示する
* 要件にないことを勝手に足さない

---

## 作業の流れ

**最初は必ず Plan モードで始める。**

1. **現状理解** — 依頼を整理し、関係ファイルを読む
2. **仕様整理** — `specs/YYYYMMDD-<slug>.md` に仕様を書く
3. **実装計画** — 触るファイル・順序・確認方法・リスクを整理する
4. **実装** — 既存の設計に合わせ、変更範囲を広げすぎない
5. **検証** — lint / test / build / 手動確認（未確認は明示する）
6. **結果共有** — 変更内容・確認内容・残課題を簡潔にまとめる

---

## 仕様ファイルのルール

* 保存先: `specs/` フォルダ（リポジトリ直下）
* ファイル名: `YYYYMMDD-<slug>.md`（例: `20250101-add-login.md`）
* テンプレート:

```md
## 仕様
- 種別: 新規作成 / 改修
- 目的:
- 現在の挙動:
- 期待する挙動:
- 対象範囲:
- 対象外:
- 完了条件:
- 確認方法:
```

---

## 新規作成 vs 改修

| | 新規作成 | 改修 |
|---|---|---|
| 最初にやること | 入出力・APIの契約を決める | 現在の挙動を再現・確認する |
| 特に確認すること | セキュリティ・権限・既存機能との整合 | 呼び出し元・API互換性・既存テストへの影響 |

どちらも: 影響範囲を確認 → 最小の変更で実施 → テストを追加・更新する

---

## 完了条件

* 実装が仕様に合っている
* 変更範囲が適切である
* 必要な確認が終わっている（未確認は明示）
* 仕様ファイル (`specs/`) が存在する

---

## やってはいけないこと

* Plan モードを飛ばして実装を始める
* 仕様ファイルを作らずに実装する
* 関係ない変更を混ぜる
* 確認せずに完了扱いにする
* 不明点を隠したまま進める

---

## 最終報告の型

```md
## 結果
- 変更内容

## 確認
- 実施した確認

## 補足
- 注意点・未対応事項
```
